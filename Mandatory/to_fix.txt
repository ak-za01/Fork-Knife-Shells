

|------------  : ------------|


handle "."
red errors input / output/ ambgues + exit status
 
$adsfa > z4 "seg"  /  exit_status 0 

shle lv go from 1->3 
exit_status 
herdoc

------------------------------------------------------------------


|------------  parsing to add to bonus later: ------------|

herdoc table + init and free of the table in the linked list.

add the building fix in "int	ft_check_buildin_cmd(char *t)"

ft_split2 from only space to all type of space , + add the ft_isspace function in utils, and also change the ft_check_string in parce.c where i add the ft_space
    , and int Exp/ft_fix_1 whwre i calee the ft_count_split_2 sisne i changed the prototype

add the expadiing change , in swap_value.c / swap_utils1.c/ swap_utils2.c and add the new function in the header + the *f =0 for the $? problem


paths = ft_split(get_env_value(env_list, "PATH"), ':');
if (!paths)
    return (NULL);     // if path is unsert excut imdiatly